name: Docker Image 


on:
  #schedule:
    #- cron: '27 15 * * *'
  #push:
    #branches: [ main ]
    # Publish semver tags as releases.
    #tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ main ]


jobs:
  job1:
   runs-on: ubuntu-latest
   steps:
     - name: Set Action Secret
       uses: hmanzur/actions-set-secret@v2.0.0
       with:
         name: 'FIRST_VALUE'
         value: 'I think this might work'
         repository: mangopraise/Dockerthem
         token: ${{ secrets.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 }}
