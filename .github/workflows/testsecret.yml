name: Docker Image 


on:
  #schedule:
    #- cron: '27 15 * * *'
  #push:
    #branches: [ main ]
    # Publish semver tags as releases.
    #tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ main ]


jobs:
  job1:
   runs-on: ubuntu-latest
   steps:
     - name: Test
       uses: hmanzur/actions-set-secret@v2.0.0
       with:
         name: 'FIRST_VALUE'
         value: 'I think this might work'
         repository: hmanzur/actions-set-secret
         token: ${{ secrets.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 }}
