name: Docker Image 


on:
  #schedule:
    #- cron: '27 15 * * *'
  #push:
    #branches: [ main ]
    # Publish semver tags as releases.
    #tags: [ 'v*.*.*' ]
  pull_request:
    branches: [ main ]


jobs:
  write:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: | 
          echo $IMAGE_NAME
        env: 
           ${{ seccrets.IMAGE_NAME}}
      
